# Disable configuration cache to avoid Gradle 9.x deprecation warnings
# (StartParameter.isConfigurationCacheRequested - scheduled for removal in Gradle 10)
org.gradle.configuration-cache=false

# Suppress deprecation warnings - the StartParameter warning comes from Gradle internals,
# not our build script. Use 'all' to see warnings when debugging.
org.gradle.warning.mode=none
